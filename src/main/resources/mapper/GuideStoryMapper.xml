<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.GuideStoryMapper">

    <resultMap id="GuideStoryResultMap" type="com.travel.pojo.vo.GuideStoryVO">
        <id column="id" property="id"/>
        <result column="author_user_id" property="authorUserId"/>
        <result column="author_name" property="authorName"/>
        <result column="author_avatar_url" property="authorAvatarUrl"/>
        <result column="is_vip" property="isVip"/>
        <result column="content" property="content"/>
        <result column="published_at" property="publishedAt"/>
        <result column="likes_count" property="likesCount"/>
        <result column="comments_count" property="commentsCount"/>
        <result column="shares_count" property="sharesCount"/>
        <result column="created_at" property="createdAt"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="selectById" resultMap="GuideStoryResultMap">
        SELECT id, author_user_id, author_name, author_avatar_url, is_vip, content,
               published_at, likes_count, comments_count, shares_count, status, created_at
        FROM traveler_stories
        WHERE id = #{id}
          AND is_deleted = 0
    </select>

    <select id="selectByUserId" resultMap="GuideStoryResultMap">
        SELECT id, author_user_id, author_name, author_avatar_url, is_vip, content,
               published_at, likes_count, comments_count, shares_count, created_at
        FROM traveler_stories
        WHERE author_user_id = #{userId}
          AND is_deleted = 0
        ORDER BY published_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1)
        FROM traveler_stories
        WHERE author_user_id = #{userId}
          AND is_deleted = 0
    </select>

    <select id="selectAll" resultMap="GuideStoryResultMap">
        SELECT id, author_user_id, author_name, author_avatar_url, is_vip, content,
               published_at, likes_count, comments_count, shares_count, status, created_at
        FROM traveler_stories
        WHERE is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY published_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(1)
        FROM traveler_stories
        WHERE is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
