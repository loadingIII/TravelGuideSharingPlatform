# 旅游攻略平台 - Java Web 得分点完成报告

## 1、项目背景及主要功能模块介绍（含登录注册在内至少3个模块）

### 项目背景
旅游攻略平台是一个基于 Spring Boot + Vue.js 的全栈 Web 应用，提供旅游攻略浏览、目的地管理、用户互动等功能。

### 主要功能模块（已完成 5+ 个模块）

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户登录注册 | ✅ | JWT 认证，支持手机号+密码登录 |
| 攻略管理 | ✅ | 攻略的增删改查、点赞、收藏 |
| 目的地管理 | ✅ | 目的地信息维护、攻略关联 |
| 用户管理 | ✅ | 用户资料、关注、个人主页 |
| 故事分享 | ✅ | 旅行故事发布、评论互动 |
| 评论系统 | ✅ | 攻略评论、故事评论 |
| 管理后台 | ✅ | 管理员登录、数据统计、内容审核 |

---

## 2、开发环境及配置（含环境配置、开发模式、全局和局部配置文件摘录）

### 开发环境

| 技术 | 版本 |
|------|------|
| Java | 17 |
| Spring Boot | 3.5.7 |
| MyBatis | 3.0.4 |
| MySQL | 8 |
| Vue | 3 |
| Vite | 5 |

### 全局配置文件

**application.yml**（核心配置摘录）：
```yaml
server:
  port: 8082

spring:
  datasource:
    username: ${mysql.username}
    password: ${mysql.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${mysql.url}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
```

---

## 3、项目包层次结构

```
src/main/java/com/travel/
├── controller/
│   ├── admin/          # 管理后台 REST API
│   ├── file/           # 文件上传控制器
│   └── user/           # 用户端 REST API
├── config/             # 配置类（WebMvc、Security、OSS）
├── filter/             # 过滤器
├── listener/           # 监听器
├── interceptor/        # 拦截器（Auth、Admin）
├── mapper/             # MyBatis Mapper 接口
├── pojo/
│   ├── common/         # 通用类（ApiResponse、PageResult）
│   ├── dto/            # 数据传输对象
│   ├── model/          # 实体模型
│   └── vo/             # 视图对象
├── security/           # JWT 认证相关
├── service/            # 业务逻辑层
│   └── impl/           # 服务实现类
└── utils/              # 工具类（OSS上传等）
```

---

## 4、各模块核心代码及测试

### 4.1 数据库连接（50分）✅ 已完成

**配置文件**：`src/main/resources/application.yml` 第 9-20 行

```yaml
spring:
  datasource:
    username: ${mysql.username}
    password: ${mysql.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${mysql.url}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
      validation-timeout: 5000
      connection-test-query: SELECT 1
```

**MyBatis 配置**：`src/main/resources/application.yml` 第 48-52 行

```yaml
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
```

**Mapper 接口示例**：`src/main/java/com/travel/mapper/GuideMapper.java`

```java
@Mapper
public interface GuideMapper {
    List<GuideSummary> listGuides(@Param("keyword") String keyword,
                                  @Param("scope") String scope,
                                  @Param("travelMode") String travelMode,
                                  @Param("sort") String sort,
                                  @Param("offset") int offset,
                                  @Param("pageSize") int pageSize);
    
    GuideDetail selectGuideDetail(@Param("guideId") Long guideId);
    
    @Delete("DELETE FROM guides WHERE id = #{id}")
    int deleteById(@Param("id") Long id);
}
```

**XML 映射文件示例**：`src/main/resources/mapper/GuideMapper.xml` 第 70-89 行

```xml
<select id="listGuides" resultMap="GuideSummaryResultMap">
    SELECT g.id, g.destination_id, g.author_id, g.title, g.summary, 
           g.cover_image_url, g.location_text, g.scope, g.travel_mode,
           g.published_at, g.days, g.budget_total, g.views_count, 
           g.likes_count, g.comments_count, g.favorites_count, g.status,
           d.name AS destination_name,
           up.nickname AS author_name,
           up.avatar_url AS author_avatar_url
    FROM guides g
    LEFT JOIN destinations d ON d.id = g.destination_id
    LEFT JOIN user_profiles up ON up.user_id = g.author_id
    <include refid="GuideSummaryWhere"/>
    <choose>
        <when test="sort == 'hot'">
            ORDER BY g.likes_count DESC, g.views_count DESC, g.id DESC
        </when>
        <otherwise>
            ORDER BY g.published_at DESC, g.id DESC
        </otherwise>
    </choose>
    LIMIT #{offset}, #{pageSize}
</select>
```

**所有 Mapper 文件**：
- `src/main/resources/mapper/GuideMapper.xml`
- `src/main/resources/mapper/GuideStoryMapper.xml`
- `src/main/resources/mapper/GuideCommentMapper.xml`
- `src/main/resources/mapper/DestinationMapper.xml`
- `src/main/resources/mapper/UserProfileMapper.xml`
- `src/main/resources/mapper/GuideItinerarySpotMapper.xml`

---

### 4.2 Cookie自动登录（5分）✅ 已完成

**实现文件**：`src/main/java/com/travel/service/impl/AdminUserServiceImpl.java`

**核心代码**：

```java
// 第 87-98 行：设置记住我Cookie
@Override
public void setRememberCookie(HttpServletResponse response, Long adminId) {
    long expiry = System.currentTimeMillis() + COOKIE_MAX_AGE * 1000L;
    String payload = adminId + "|" + expiry + "|" + UUID.randomUUID();
    String signature = sign(payload);
    String token = Base64.getUrlEncoder().withoutPadding().encodeToString(
            (payload + "|" + signature).getBytes(StandardCharsets.UTF_8));
    Cookie cookie = new Cookie(COOKIE_NAME, token);
    cookie.setPath("/");
    cookie.setHttpOnly(true);
    cookie.setMaxAge(COOKIE_MAX_AGE);
    response.addCookie(cookie);
}

// 第 100-127 行：验证Cookie恢复会话
@Override
public boolean tryRestoreSession(HttpServletRequest request, HttpServletResponse response, HttpSession session) {
    String token = getCookieValue(request, COOKIE_NAME);
    if (token == null) return false;
    try {
        String decoded = new String(Base64.getUrlDecoder().decode(token), StandardCharsets.UTF_8);
        String[] parts = decoded.split("\\|");
        if (parts.length != 4) return false;
        long adminId = Long.parseLong(parts[0]);
        long expiry = Long.parseLong(parts[1]);
        String signature = parts[3];
        String payload = parts[0] + "|" + parts[1] + "|" + parts[2];
        if (!verify(payload, signature)) return false;
        if (System.currentTimeMillis() > expiry) return false;
        AdminUser admin = adminUserMapper.selectById(adminId);
        if (admin == null || admin.getStatus() != 1) return false;
        setSession(session, admin);
        return true;
    } catch (Exception e) {
        return false;
    }
}

// 第 138-146 行：HMAC-SHA256签名
private String sign(String data) {
    try {
        Mac mac = Mac.getInstance("HmacSHA256");
        mac.init(new SecretKeySpec(hmacSecret.getBytes(StandardCharsets.UTF_8), "HmacSHA256"));
        return Base64.getUrlEncoder().withoutPadding().encodeToString(
            mac.doFinal(data.getBytes(StandardCharsets.UTF_8)));
    } catch (Exception e) {
        throw new RuntimeException(e);
    }
}
```

**功能说明**：
- 使用 HMAC-SHA256 签名验证 Cookie 完整性
- Cookie 有效期 7 天（第 39 行：`COOKIE_MAX_AGE = 7 * 24 * 60 * 60`）
- HttpOnly 标记防止 XSS 攻击（第 95 行）

---

### 4.3 文件上传下载（各至少1个，10分）

#### 文件上传（5分）✅ 已完成

**实现文件**：`src/main/java/com/travel/controller/file/FileController.java` 第 18-30 行

```java
@RestController
@RequestMapping("/api/files")
@RequiredArgsConstructor
public class FileController {

    private final OssUploadUtil ossUploadUtil;

    @PostMapping("/upload")
    public ApiResponse<String> upload(@RequestParam("file") MultipartFile file) {
        if (file.isEmpty()) {
            return ApiResponse.fail("FILE_EMPTY", "文件不能为空");
        }
        try {
            String url = ossUploadUtil.upload(file);
            return ApiResponse.success(url);
        } catch (Exception e) {
            log.error("文件上传失败", e);
            return ApiResponse.fail("UPLOAD_FAIL", "文件上传失败");
        }
    }
}
```

**上传工具类**：`src/main/java/com/travel/utils/OssUploadUtil.java` - 阿里云 OSS 存储实现

**配置文件**：`src/main/resources/application.yml` 第 57-62 行

```yaml
app:
  oss:
    endpoint: ${oss.endpoint}
    access-key-id: ${oss.access-key-id}
    access-key-secret: ${oss.access-key-secret}
    bucket-name: ${oss.bucket-name}
```

#### 文件下载（5分）❌ 未实现

**原因**：当前业务暂不需要文件下载功能，用户通过 OSS URL 直接访问文件。

---

### 4.4 监听器（至少1个，5分）✅ 已完成

**实现文件**：`src/main/java/com/travel/listener/OnlineUserListener.java`

```java
@Slf4j
@Component
public class OnlineUserListener implements HttpSessionListener {

    private static final AtomicInteger onlineCount = new AtomicInteger(0);

    @Override
    public void sessionCreated(HttpSessionEvent event) {
        int count = onlineCount.incrementAndGet();
        event.getSession().getServletContext().setAttribute("onlineUserCount", count);
        log.info("用户上线 | 当前在线: {}", count);
    }

    @Override
    public void sessionDestroyed(HttpSessionEvent event) {
        int count = onlineCount.decrementAndGet();
        event.getSession().getServletContext().setAttribute("onlineUserCount", count);
        log.info("用户下线 | 当前在线: {}", count);
    }

    public static int getOnlineCount() {
        return onlineCount.get();
    }
}
```

**功能说明**：
- 实现 `HttpSessionListener` 接口
- 统计在线用户数，存储在 `ServletContext`
- 在管理后台仪表盘显示（`dashboard.jsp` 第 54 行：`${onlineUserCount}`）

---

### 4.5 过滤器（至少1个，5分）✅ 已完成

**实现文件**：`src/main/java/com/travel/filter/RequestLoggingFilter.java`

```java
@Slf4j
@Component
@Order(1)
public class RequestLoggingFilter extends OncePerRequestFilter {

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain chain)
            throws ServletException, IOException {

        long start = System.currentTimeMillis();

        chain.doFilter(request, response);

        long duration = System.currentTimeMillis() - start;

        String method = request.getMethod();
        String uri = request.getRequestURI();
        String query = request.getQueryString();
        String ip = request.getRemoteAddr();
        String ua = request.getHeader("User-Agent");
        int status = response.getStatus();

        if (query != null) {
            uri += "?" + query;
        }

        log.info("[REQUEST] {} {} | IP: {} | Status: {} | {}ms | UA: {}",
                method, uri, ip, status, duration, 
                ua != null ? ua.substring(0, Math.min(ua.length(), 50)) : "N/A");
    }
}
```

**功能说明**：
- 继承 `OncePerRequestFilter` 确保每次请求只执行一次
- 记录请求方法、URI、IP、状态码、响应时间、User-Agent
- 使用 `@Order(1)` 设置过滤器优先级

---

### 4.6 表达式语言（EL）（至少3种，5分）✅ 已完成

**EL 表达式类型及示例**：

**1. 简单变量** - `dashboard.jsp` 第 12 行
```jsp
<h2>${userCount}</h2>
```

**2. 对象属性访问** - `user/list.jsp` 第 20 行
```jsp
<td>${u.username}</td>
```

**3. 隐式对象** - `login.jsp` 第 23 行
```jsp
<form method="post" action="${pageContext.request.contextPath}/admin/login">
```

**4. 条件运算** - `user/list.jsp` 第 22 行
```jsp
<td>${u.status == 1 ? '正常' : '禁用'}</td>
```

**5. 方法调用** - `story/list.jsp` 第 20 行
```jsp
<td>${s.content.length() > 50 ? s.content.substring(0, 50).concat('...') : s.content}</td>
```

**6. 集合操作** - `user/list.jsp` 第 16 行
```jsp
<c:forEach var="u" items="${pageResult.list}">
```

---

### 4.7 标签（JSTL）（至少3种，5分）✅ 已完成

**引入方式**：各 JSP 文件第 2 行
```jsp
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
```

**1. `<c:if>` 条件判断** - `user/list.jsp` 第 8-10 行
```jsp
<c:if test="${not empty msg}">
    <div class="alert alert-success">${msg}</div>
</c:if>
```

**2. `<c:forEach>` 遍历集合** - `user/list.jsp` 第 16-32 行
```jsp
<c:forEach var="u" items="${pageResult.list}">
    <tr>
        <td>${u.id}</td>
        <td>${u.username}</td>
        <td>${u.phone}</td>
        <td>${u.email}</td>
        <td>${u.status == 1 ? '正常' : '禁用'}</td>
        <td>${u.createdAt}</td>
        <td>
            <form method="post" action="${pageContext.request.contextPath}/admin/users/delete/${u.id}" 
                  style="display:inline" onsubmit="return confirm('确认删除？')">
                <button type="submit" class="btn btn-sm btn-danger">删除</button>
            </form>
        </td>
    </tr>
</c:forEach>
```

**3. `<c:forEach>` 分页循环** - `user/list.jsp` 第 35-41 行
```jsp
<c:forEach begin="1" end="${pageResult.totalPages}" var="i">
    <li class="page-item ${i == pageResult.page ? 'active' : ''}">
        <a class="page-link" href="${pageContext.request.contextPath}/admin/users?page=${i}">${i}</a>
    </li>
</c:forEach>
```

**4. `<jsp:include>` 页面包含** - `dashboard.jsp` 第 3-5 行
```jsp
<jsp:include page="common/header.jsp">
    <jsp:param name="active" value=""/>
</jsp:include>
```

---

### 4.8 安全性（登录注册中考查）✅ 已完成

**1. 密码加密** - `SecurityConfig.java` 第 12 行

```java
@Configuration
public class SecurityConfig {
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

**2. JWT 认证拦截器** - `AuthInterceptor.java` 第 19-49 行

```java
@Override
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
    if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
        return true;
    }

    if (!(handler instanceof HandlerMethod handlerMethod)) {
        return true;
    }

    RequireLogin requireLogin = handlerMethod.getMethodAnnotation(RequireLogin.class);
    if (requireLogin == null) {
        requireLogin = handlerMethod.getBeanType().getAnnotation(RequireLogin.class);
    }
    if (requireLogin == null) {
        return true;
    }

    String authHeader = request.getHeader("Authorization");
    if (!StringUtils.hasText(authHeader) || !authHeader.startsWith("Bearer ")) {
        throw new BusinessException(ErrorCode.UNAUTHORIZED, "请先登录");
    }

    String token = authHeader.substring("Bearer ".length()).trim();
    if (!StringUtils.hasText(token)) {
        throw new BusinessException(ErrorCode.UNAUTHORIZED, "无效的登录凭证");
    }

    LoginUser loginUser = jwtTokenService.parseAccessToken(token);
    UserContext.set(loginUser);
    return true;
}
```

**3. HttpOnly Cookie** - `AdminUserServiceImpl.java` 第 95 行

```java
cookie.setHttpOnly(true);
```

**4. 输入验证** - `AuthController.java` 第 35、47 行

```java
@PostMapping("/register")
public ApiResponse<AuthTokenVO> register(@Valid @RequestBody AuthRegisterDTO request) {
    return ApiResponse.success(authService.register(request));
}

@PostMapping("/login")
public ApiResponse<AuthTokenVO> login(@Valid @RequestBody AuthLoginDTO request) {
    return ApiResponse.success(authService.login(request));
}
```

---

## 5、加分项检查

| 加分项 | 状态 | 说明 | 核心文件 |
|--------|------|------|----------|
| SSM 框架 | ✅ | Spring Boot + MyBatis | `pom.xml` |
| JWT 认证 | ✅ | 用户端使用 JWT Token | `AuthInterceptor.java` |
| RESTful API | ✅ | 统一响应格式 `ApiResponse<T>` | `pojo/common/ApiResponse.java` |
| 分页查询 | ✅ | `PageResult<T>` 分页封装 | `pojo/common/PageResult.java` |
| 前后端分离 | ✅ | Vue 3 + Spring Boot | `frontend/` 目录 |

---

## 6、总结

### 得分点完成情况

| 得分点 | 分值 | 完成状态 | 得分 | 核心文件 |
|--------|------|----------|------|----------|
| 项目背景及功能模块 | - | ✅ 7个模块 | - | - |
| 开发环境及配置 | - | ✅ 完整 | - | `application.yml` |
| 项目包层次结构 | - | ✅ 清晰分层 | - | `src/main/java/com/travel/` |
| 数据库连接 | 50分 | ✅ MySQL + MyBatis | 50分 | `application.yml` 第9-20行<br>`GuideMapper.xml` 第70-89行 |
| Cookie自动登录 | 5分 | ✅ HMAC签名Cookie | 5分 | `AdminUserServiceImpl.java` 第87-146行 |
| 文件上传 | 5分 | ✅ 阿里云OSS | 5分 | `FileController.java` 第18-30行 |
| 文件下载 | 5分 | ❌ 暂不需要 | 0分 | - |
| 监听器 | 5分 | ✅ HttpSessionListener | 5分 | `OnlineUserListener.java` 第12-33行 |
| 过滤器 | 5分 | ✅ OncePerRequestFilter | 5分 | `RequestLoggingFilter.java` 第17-42行 |
| 表达式语言 | 5分 | ✅ 6种以上用法 | 5分 | `dashboard.jsp` 第12行<br>`user/list.jsp` 第20-22行 |
| 标签 | 5分 | ✅ 4种标签 | 5分 | `user/list.jsp` 第8-41行<br>`dashboard.jsp` 第3-5行 |
| 安全性 | - | ✅ BCrypt+JWT+HttpOnly | - | `SecurityConfig.java` 第12行<br>`AuthInterceptor.java` 第19-49行 |
| SSM框架加分 | 5-10分 | ✅ Spring Boot + MyBatis | 5-10分 | `pom.xml` |

### 预计总分

**基础分**：85分（满分90分，扣5分文件下载）
**加分项**：5-10分
**预计总分**：90-95分

### 待补充材料

1. **测试截图**：
   - 登录页面截图（`/admin/login.html`）
   - 管理后台仪表盘截图（`/admin/index.html`）
   - 文件上传功能测试截图

2. **IDE截图**：
   - 项目包层次结构截图（IntelliJ IDEA 或 Eclipse）
   - 数据库连接配置截图

---

**报告生成时间**：2026-05-20
